---
import Layout from "@/layouts/Layout.astro";
import Container from "@/components/container.astro";
import Breadcrumbs from "@/components/ui/Breadcrumbs.astro";
import { Icon } from "astro-icon/components";
---

<Layout 
  title="おおさきラボ" 
  description="おおさきくんが自分用に作った、ちょっと便利な開発者向けツール置き場です。"
>
  <Container>
    <Breadcrumbs items={[{ label: "おおさきラボ" }]} />
    <div class="hero-section">
      <div class="hero-content-wrapper">
        <div class="hero-text-box">
          <h1 class="hero-title">おおさきラボ</h1>
          <p class="hero-intro">
            おおさきくんの研究所。<br />ワールドづくりとアバターづくりに便利ないろいろツール集！
          </p>
        </div>
      </div>
    </div>
  </Container>

  <Container>
    <div class="tools-grid">
      <a href="/lab/vector-tracer" class="tool-card">
        <div class="tool-icon">🖋️</div>
        <div class="tool-info">
          <h2 class="tool-name">いろあつめーかー</h2>
          <p class="tool-desc">写真やイラストをきれいな線のベクター画像（SVG）に変換します。</p>
        </div>
      </a>

      <a href="/lab/msdf-generator" class="tool-card">
        <div class="tool-icon">💠</div>
        <div class="tool-info">
          <h2 class="tool-name">でかーるめーかー</h2>
          <p class="tool-desc">SVGをMSDFにします。超軽量のテクスチャでベクターっぽい奇麗なデカールが貼れます。liltoonなどが対応してるよ！</p>
        </div>
      </a>

      <a href="/lab/image-scatter" class="tool-card">
        <div class="tool-icon">🌌</div>
        <div class="tool-info">
          <h2 class="tool-name">ちりばめーかー</h2>
          <p class="tool-desc">画像をランダムにちりばめて新しい画像を生成します。</p>
        </div>
      </a>

      <a href="/lab/icon-generator" class="tool-card">
        <div class="tool-icon">🎨</div>
        <div class="tool-info">
          <h2 class="tool-name">あいこんめーかー</h2>
          <p class="tool-desc">1つの画像から、様々な用途に使えるアイコンサイズを一括生成します。</p>
        </div>
      </a>
    </div>

    <div class="legal-section-link">
      <div class="legal-info">
        <h3 class="legal-title">ご利用について</h3>
        <p class="legal-text">
          ラボのツールはすべてブラウザ上で安全に動作します。<br />
          利用規約（権利や免責事項）については、以下のページをご確認ください。
        </p>
      </div>
      <a href="/about/legal#lab-terms" class="btn-legal">
        <Icon name="mdi:text-box-search-outline" class="btn-icon" />
        利用規約を確認する
      </a>
    </div>
  </Container>
</Layout>

<style>
  /* --- Hero Section --- */
  .hero-section {
    padding: 3rem 1.5rem;
    position: relative;
    background-image: linear-gradient(rgba(255, 255, 255, 0.45), rgba(255, 255, 255, 0.45)), url('/images/labo_hero.webp');
    background-size: cover;
    background-position: center;
    border-radius: 2rem;
    overflow: hidden;
    margin-bottom: 4rem;
    border: 3px solid var(--main-color);
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 400px;
  }

  .hero-content-wrapper {
    max-width: 800px;
    width: 100%;
    z-index: 10;
  }

  .hero-text-box {
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(12px);
    padding: 3rem;
    border-radius: 2rem;
    border: 2px solid var(--main-color);
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    text-align: center;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 900;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    color: var(--main-color);
    text-shadow: 2px 2px 0px white, -1px -1px 0px white, 1px -1px 0px white, -1px 1px 0px white;
  }

  .hero-intro {
    font-size: clamp(1.1rem, 2vw, 1.25rem);
    color: var(--sub-color);
    line-height: 1.6;
    font-weight: 800;
  }

  /* --- Tools Grid --- */
  .tools-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 5rem;
  }

  .tool-card {
    background: white;
    border: 2px solid var(--main-color);
    border-radius: 2rem;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    position: relative;
    box-shadow: 0 10px 0 rgba(0,0,0,0.05);
    height: 100%;
  }

  .tool-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 0 rgba(0,0,0,0.05);
  }

  .tool-icon {
    font-size: 3rem;
    margin-bottom: 1.5rem;
    background: var(--bg-color);
    width: 5rem;
    height: 5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
  }

  .tool-name {
    font-size: 1.5rem;
    font-weight: 900;
    color: var(--main-color);
    margin-bottom: 0.75rem;
  }

  .tool-desc {
    font-size: 0.95rem;
    color: var(--sub-color);
    line-height: 1.6;
    margin-bottom: 1.5rem;
    opacity: 0.8;
  }

  /* --- Legal Section (Matches products/index.astro CTA) --- */
  .legal-section-link {
    background: white;
    color: var(--sub-color);
    padding: 4rem;
    border-radius: 2rem;
    text-align: center;
    margin-bottom: 4rem;
    border: 2px solid var(--main-color);
    box-shadow: none;
    max-width: none; /* Full width */
  }

  /* Remove flex wrapper behavior if it exists */
  .legal-info {
    margin-bottom: 0;
  }

  .legal-title {
    font-size: 2rem;
    font-weight: 900;
    margin-bottom: 1rem;
    color: var(--main-color);
  }

  .legal-text {
    margin-bottom: 0;
    color: var(--sub-color);
    opacity: 0.8;
    line-height: 1.6;
    font-size: 1rem;
  }

  .btn-legal {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    background: var(--main-color);
    color: white;
    padding: 1rem 2.5rem;
    border-radius: 3rem;
    font-weight: 900;
    text-decoration: none;
    margin-top: 2rem;
    transition: all 0.3s ease;
    font-size: 1rem;
    box-shadow: none;
    border: none;
  }

  .btn-legal:hover {
    background: var(--sub-color);
    transform: translateY(-2px);
    box-shadow: none;
  }
  
  .btn-icon {
    font-size: 1.25rem;
  }

  @media (max-width: 768px) {
    .legal-section-link {
      padding: 3rem 1.5rem;
    }
  }
</style>
