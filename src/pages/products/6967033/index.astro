---
import Layout from '../../../layouts/Layout.astro';
import Container from '../../../components/container.astro';
import Breadcrumbs from '../../../components/ui/Breadcrumbs.astro';

const item = {
  id: "6967033",
  name: "【ベースステーションの電源切替】BaseStation Remocon【無料試用版あり】",
  booth_url: "https://booth.pm/ja/items/6967033",
  sample_world_url: "",
  published_at: "2025年5月25日 21時30分",
  main_image: "/images/products/6967033/1.webp",
  tags: ["ソフトウェア・ハードウェア", "ソフトウェア", "Android", "VRChat", "SteamVR", "BaseStation", "Android x ソフトウェア"]
};

const description = `⚡🔲 BaseStation Remocon 🔲⚡

VRChatter向け ベースステーション電源管理アプリ　(Android専用)
🎮 Quest・PICO + VIVE・Tundra Tracker 環境でのベースステーション電源管理に！🔋✨

# 📱 概要
このアプリは、ベースステーション（Base Station 2.0）をBluetooth経由で制御し、電源のオン・オフを簡単に行います。

# ✨ 特徴
- 🔴 ワンタップでON/OFF: 大きなボタンで直感的操作
- 📋 実行ログ表示: 動作状況が一目でわかる
- 🔍 自動デバイス検出: 近くのベースステーションを自動で見つけます
- 🚀 高速起動: アプリ起動から操作まで数秒

# ⚠️ 重要な注意事項
# 🛡️ 動作確認について
このアプリは機器の相性問題が発生する可能性があります。
- スマートフォンの機種・ベースステーションのバージョンによって動作しない場合があります
- ⚠️必ず 試用版で動作確認 を行ってから製品版をご検討ください⚠️
- 動作しない場合の返金は承れません
⚠️ 類似動作をする他のアプリ/ソフトウェアと併用を避けてください。問題が発生したら、ベースステーションの電源を抜いて再起動して下さい

# 📋 動作確認済み環境
- Pixel7(Android 15)
- Bluetooth v5.2
- SteamVR Base Station 2.0

# 🚫 非対応
- Base Station 1.0
- iOS（iPhone/iPad）

# 📦 配布内容

# 🆓 無料版（動作確認用）
- 全機能利用可能
- 機器相性の確認用
- アプリ内に「試用版」表示

# 💰 製品版
- 機能は無料版と同じ
- 開発継続支援版
- 「試用版」表示なし

# 📥 導入方法
1️⃣ APKファイルの準備
1. ダウンロードしたZIPファイルを解凍
2. 以下のAPKファイルを確認
➡ BaseStationRemocon_Trial.apk
➡ BaseStationRemocon_Full.apk


2️⃣ スマートフォンの設定
1. 設定 → アプリ → 特別なアプリアクセス → 不明なアプリのインストール → 許可
※ 機種により表記が異なります
2. Google Playプロテクトの警告が出た場合は「詳細」→「インストール」を選択

3️⃣ APKのインストール
1. APKファイルをスマートフォンに転送
2. ファイルマネージャーでAPKファイルをタップ
3. インストール画面で「インストール」をタップ
4. 完了後「開く」でアプリを起動
（インストール後にホーム画面にアプリが表示されない場合は、「BaseStation Remocon」で一覧から探すと出てきますので、必要に応じてホーム画面にショートカットを作成してご利用ください）


# 🔧 使い方
1. アプリを起動
2. 権限の承認（⚠️ Bluetoothから位置を推定可能であるため、位置の取得と表示されます。）
3. ONまたはOFFボタンをタップ
4. 実行ログで結果を確認

# ⚡ トラブルシューティング
❓ ベースステーションが見つからない
- スマートフォンのBluetooth設定を確認
- ベースステーションとの距離を近づける
- アプリを再起動してみる

# ❓ 電源が切れない/入らない
- ベースステーションが省電力モードになっていないか確認
- SteamVRが起動中でないか確認
- 他のデバイスから制御されていないか確認
- V1でないか確認

# ❓ アプリが動作しない
- Android版のバージョンを確認
- 他のBluetooth機器との干渉を確認
- 無料版で動作テストを実施

# ❓ APKがインストールできない
- 提供元不明のアプリのインストール許可を確認
- 古いバージョンのアンインストール
- ストレージ容量の確認

# 📞 サポート

# 🐛 不具合報告・要望
- Boothショップメッセージまで

# ⚠️ サポート範囲
- アプリの基本機能に関する質問
- 明らかなバグの修正
- 機能改善の提案

# 🚫 サポート対象外
- 機器相性による動作不良
- Android OS固有の問題
- 他社製品との互換性

# 📜 ライセンス・利用規約

# 🆗 許可事項
- 個人・商用問わず利用可能
- アプリの改変・再配布は禁止

# 🚫 禁止事項
- 悪意のある用途での使用
- 作者のなりすまし行為

# 🛡️ 免責事項
- 本アプリの利用によるいかなる損害について、作者は責任を負いません
- ベースステーションやその他機器の故障について、作者は責任を負いません

# 🔄 更新履歴
🟢 v1.0.0 (2025/05/25)
- 初回リリース
- ベースステーション ON/OFF 機能
- 実行ログ表示機能

〰️〰️〰️〰️〰️〰️〰️〰️〰️〰️〰️〰️

💝 このアプリが役に立ちましたら、製品版でのサポートをご検討ください！

開発継続のモチベーションになります 🚀`;
const faq = ``;

// Markdownを簡易的にHTMLに変換
function parseMarkdown(md: string): string {
  if (!md) return '';
  
  const lines = md.split('\n');
  const result: string[] = [];
  let listStack: number[] = []; // インデントレベルのスタック
  
  const getIndent = (line: string): number => {
    if (line.startsWith('      - ')) return 3;
    if (line.startsWith('    - ')) return 2;
    if (line.startsWith('  - ')) return 1;
    if (line.trim().startsWith('- ')) return 0;
    return -1;
  };
  
  const closeListsToLevel = (targetLevel: number) => {
    while (listStack.length > 0 && listStack[listStack.length - 1] >= targetLevel) {
      listStack.pop();
      result.push('</ul>');
    }
  };
  
  const closeAllLists = () => {
    while (listStack.length > 0) {
      listStack.pop();
      result.push('</ul>');
    }
  };
  
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    const trimmed = line.trim();
    const indent = getIndent(line);
    
    // 空行の処理
    if (!trimmed) {
      closeAllLists();
      result.push('');
      continue;
    }
    
    // 見出しの処理
    if (trimmed.startsWith('### ')) {
      closeAllLists();
      result.push('<h3>' + trimmed.substring(4) + '</h3>');
      continue;
    }
    if (trimmed.startsWith('## ')) {
      closeAllLists();
      result.push('<h2>' + trimmed.substring(3) + '</h2>');
      continue;
    }
    if (trimmed.startsWith('# ')) {
      closeAllLists();
      result.push('<h1>' + trimmed.substring(2) + '</h1>');
      continue;
    }
    
    // リストの処理
    if (indent >= 0) {
      const content = trimmed.substring(2);
      
      // 現在のインデントレベルより深い場合、リストを閉じる
      if (listStack.length > 0 && indent < listStack[listStack.length - 1]) {
        closeListsToLevel(indent + 1);
      }
      
      // 新しいレベルの場合、ulを開く
      if (listStack.length === 0 || indent > listStack[listStack.length - 1]) {
        result.push('<ul>');
        listStack.push(indent);
      }
      
      // リスト項目を追加
      result.push('<li>' + content);
      
      // 次の行を先読みして、子リストがあるかチェック
      const nextIndent = i + 1 < lines.length ? getIndent(lines[i + 1]) : -1;
      if (nextIndent <= indent || nextIndent === -1) {
        result.push('</li>');
      }
      
      continue;
    }
    
    // その他の行（リストを終了）
    closeAllLists();
    
    // 通常の段落
    result.push('<p>' + trimmed + '</p>');
  }
  
  // 最後にリストが残っている場合
  closeAllLists();
  
  return result.join('\n');
}

const breadcrumbs = [
  { label: 'ホーム', href: '/' },
  { label: '商品一覧', href: '/products' },
  { label: item.name, href: `/products/${item.id}` }
];
---

<Layout title={item.name}>
  <Container>
    <Breadcrumbs items={breadcrumbs} />
  </Container>
  
  <main class="product-detail">
    <div class="product-header">
      <div class="product-image">
        <img src={item.main_image} alt={item.name} />
      </div>
      
      <div class="product-info">
        <h1>{item.name}</h1>
        
        {item.published_at && (
          <p class="published-date">📅 公開日: {item.published_at}</p>
        )}
        
        <div class="action-buttons">
          {item.booth_url && (
            <a href={item.booth_url} target="_blank" rel="noopener noreferrer" class="booth-link">
              <img src="/images/booth-icon.svg" alt="Booth" class="booth-svg booth-icon" />
            </a>
          )}
          
          {item.sample_world_url && (
            <a href={item.sample_world_url} target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
              <img src="/images/world.svg" alt="World" class="btn-icon" />
              サンプルワールド
            </a>
          )}
        </div>
        
        {item.tags && item.tags.length > 0 && (
          <div class="tags">
            {item.tags.map(tag => (
              <span class="tag">{tag}</span>
            ))}
          </div>
        )}
      </div>
    </div>
    
    <section class="product-variations">
      <h2>💰 価格・バリエーション</h2>
      <div class="variations">
  <div class="variation-item">
    <h4>試用版(動作確認用)</h4>
    <div class="price">¥0</div>
  </div>
  <div class="variation-item">
    <h4>製品版</h4>
    <div class="price">¥550</div>
  </div>
</div>

    </section>
    
    <section class="product-description">
      <h2>📝 商品説明</h2>
      <div class="description-content" set:html={parseMarkdown(description)}></div>
    </section>
    
    <section class="image-gallery">
<h2>📷 ギャラリー</h2>
<div class="gallery-grid">
  <img src="/images/products/6967033/1.webp" alt="【ベースステーションの電源切替】BaseStation Remocon【無料試用版あり】" />
  <img src="/images/products/6967033/2.webp" alt="【ベースステーションの電源切替】BaseStation Remocon【無料試用版あり】" />
  <img src="/images/products/6967033/0.webp" alt="【ベースステーションの電源切替】BaseStation Remocon【無料試用版あり】" />
</div>
</section>

    
    
    
  </main>
    <div id="image-modal" class="modal">
      <span class="close">&times;</span>
      <img class="modal-content" id="modal-img">
    </div>
  </main>
</Layout>

<script is:inline>
  // Get the modal
  var modal = document.getElementById("image-modal");
  var modalImg = document.getElementById("modal-img");
  
  // Get all gallery images
  var images = document.querySelectorAll('.gallery-grid img');
  
  images.forEach(function(img) {
    img.onclick = function() {
      if (modal && modalImg) {
        modal.style.display = "flex";
        modalImg.src = this.src;
      }
    }
  });

  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];

  // When the user clicks on <span> (x), close the modal
  if (span) {
    span.onclick = function() {
      modal.style.display = "none";
    }
  }
  
  // Close on click outside
  if (modal) {
    modal.onclick = function(event) {
      if (event.target === modal) {
          modal.style.display = "none";
      }
    }
  }
  
  // Close on Escape key
  document.addEventListener('keydown', function(event) {
    if (event.key === "Escape" && modal && modal.style.display === "flex") {
        modal.style.display = "none";
    }
  });
</script>

<style>
  .product-detail {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }
  
  .product-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-bottom: 3rem;
  }
  
  .product-image img {
    width: 100%;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
  
  .product-info h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: var(--color-text-heading);
  }
  
  .published-date {
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
  }
  
  .action-buttons {
    display: flex;
    gap: 1rem;
    align-items: center;
    margin-bottom: 1.5rem;
  }
  
  .btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
  }
  
  .btn-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
  }
  
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 12px rgba(102, 126, 234, 0.4);
  }
  
  .booth-link {
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    height: 48px;
  }
  
  .booth-link:hover {
    transform: scale(1.1);
    opacity: 0.8;
  }
  
  .booth-svg {
    height: 48px;
    width: auto;
    display: block;
    object-fit: contain;
  }
  
  .btn-secondary {
    background: linear-gradient(135deg, #4A90E2 0%, #357ABD 100%);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    height: 48px;
  }
  
  .btn-secondary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 12px rgba(74, 144, 226, 0.4);
  }
  
  .btn-icon {
    height: 24px;
    width: auto;
  }
  
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
  
  .tag {
    background: var(--color-bg-secondary);
    padding: 0.25rem 0.75rem;
    border-radius: 16px;
    font-size: 0.875rem;
    color: var(--color-text-muted);
  }
  
  section {
    margin-bottom: 3rem;
  }
  
  section h2 {
    font-size: 1.75rem;
    margin-bottom: 1.5rem;
    color: var(--color-text-heading);
  }
  
  .variations {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
  }
  
  .variation-item {
    background: var(--color-bg-secondary);
    padding: 1.5rem;
    border-radius: 12px;
    border: 2px solid transparent;
    transition: all 0.3s ease;
  }
  
  .variation-item:hover {
    border-color: var(--color-primary);
    transform: translateY(-4px);
  }
  
  .variation-item h4 {
    margin-bottom: 0.5rem;
    color: var(--color-text-heading);
  }
  
  .variation-item .price {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: 0.5rem;
  }
  
  .variation-item .detail {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin: 0.25rem 0;
  }
  
  /* Markdown content styling */
  .description-content,
  .faq-content {
    line-height: 1.8;
    color: var(--color-text);
  }
  
  .description-content :global(h1),
  .faq-content :global(h1) {
    font-size: 1.75rem;
    font-weight: 800;
    margin-top: 2rem;
    margin-bottom: 0.75rem;
    color: var(--main-color);
    border-bottom: 3px solid var(--main-color);
    padding-bottom: 0.5rem;
  }
  
  .description-content :global(h2),
  .faq-content :global(h2) {
    font-size: 1.5rem;
    font-weight: 700;
    margin-top: 1.75rem;
    margin-bottom: 0.625rem;
    color: var(--sub-color);
    border-left: 4px solid var(--main-color);
    padding-left: 1rem;
  }
  
  .description-content :global(h3),
  .faq-content :global(h3) {
    font-size: 1.25rem;
    font-weight: 700;
    margin-top: 1.5rem;
    margin-bottom: 0.5rem;
    color: var(--sub-color);
  }
  
  .description-content :global(p),
  .faq-content :global(p) {
    margin-bottom: 1rem;
  }
  
  .description-content :global(ul),
  .description-content :global(ol),
  .faq-content :global(ul),
  .faq-content :global(ol) {
    margin-bottom: 1rem;
    padding-left: 2rem;
    list-style-position: outside;
  }
  
  .description-content :global(ul) {
    list-style-type: disc;
  }
  
  .faq-content :global(ul) {
    list-style-type: disc;
  }
  
  .description-content :global(li),
  .faq-content :global(li) {
    margin-bottom: 0.5rem;
    display: list-item;
  }
  
  .description-content :global(code),
  .faq-content :global(code) {
    background: var(--bg-color);
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-family: 'Courier New', monospace;
    font-size: 0.9em;
  }
  
  .description-content :global(pre),
  .faq-content :global(pre) {
    background: var(--bg-color);
    padding: 1rem;
    border-radius: 8px;
    overflow-x: auto;
    margin-bottom: 1rem;
  }
  
  .description-content :global(blockquote),
  .faq-content :global(blockquote) {
    border-left: 4px solid var(--main-color);
    padding-left: 1rem;
    margin: 1rem 0;
    font-style: italic;
    color: var(--sub-color);
  }
  
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1rem;
  }
  
  .gallery-grid img {
    width: 100%;
    border-radius: 8px;
    transition: transform 0.3s ease;
  }
  
  .gallery-grid img:hover {
    transform: scale(1.05);
  }
  
  .tech-info {
    background: var(--color-bg-secondary);
    padding: 2rem;
    border-radius: 12px;
  }
  
  .tech-info h3 {
    font-size: 1.25rem;
    margin-bottom: 1rem;
    color: var(--color-text-heading);
  }
  
  .tech-info ul {
    list-style: none;
    padding: 0;
  }
  
  .tech-info li {
    padding: 0.5rem 0;
    border-bottom: 1px solid var(--color-border);
  }
  
  .tech-info li:last-child {
    border-bottom: none;
  }
  
  .faq-content {
    line-height: 1.8;
  }
  
  @media (max-width: 768px) {
    .product-header {
      grid-template-columns: 1fr;
    }
    
    .action-buttons {
      flex-direction: column;
    }
    
    .btn {
      text-align: center;
    }
  }
  /* Modal styles */
  .modal {
    display: none;
    position: fixed;
    z-index: 1000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto;
    background-color: rgba(0,0,0,0.9);
    align-items: center;
    justify-content: center;
  }
  
  .modal-content {
    margin: auto;
    display: block;
    max-width: 90%;
    max-height: 90%;
    object-fit: contain;
    border-radius: 4px;
  }
  
  /* Animation */
  .modal-content {
    animation-name: zoom;
    animation-duration: 0.3s;
  }
  
  @keyframes zoom {
    from {transform:scale(0)} 
    to {transform:scale(1)}
  }
  
  .close {
    position: absolute;
    top: 15px;
    right: 35px;
    color: #f1f1f1;
    font-size: 40px;
    font-weight: bold;
    transition: 0.3s;
    cursor: pointer;
    z-index: 1001;
  }
  
  .close:hover,
  .close:focus {
    color: #bbb;
    text-decoration: none;
    cursor: pointer;
  }
  
  .gallery-grid img {
    cursor: pointer;
  }
</style>
