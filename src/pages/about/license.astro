---
import Layout from "@/layouts/Layout.astro";
import Container from "@/components/container.astro";
import { Icon } from "astro-icon/components";
import Breadcrumbs from "@/components/ui/Breadcrumbs.astro";
---

<Layout title="ライセンス">
  <div class="page-header">
    <Container>
      <Breadcrumbs items={[
        { label: "ライセンス" }
      ]} />
      <h1 class="page-title">大崎商会 製品利用規約</h1>
      <p class="page-description">
        本製品をご利用いただく際は、本規約に同意したものとみなします。
      </p>
    </Container>
  </div>

  <Container>
    <div class="license-content">
      <!-- Section 1: 利用可能範囲 -->
      <section class="license-section">
        <div class="section-header">
          <Icon name="mdi:check-decagram" class="section-icon allowed" />
          <h2 class="section-title">1. 利用可能範囲</h2>
        </div>
        <div class="section-body">
          <p class="section-intro">
            本製品は、VRChat等のメタバース空間におけるワールドやアバター用アセットとしての利用を想定しています。
          </p>
          <ul class="allowed-list">
            <li>
              <Icon name="mdi:check" class="list-icon" />
              <span>個人・法人を問わず、営利・非営利目的で利用可能です。</span>
            </li>
            <li>
              <Icon name="mdi:check" class="list-icon" />
              <span>動画配信、ゲーム制作、広告物等への利用が可能です。</span>
            </li>
            <li>
              <Icon name="mdi:check" class="list-icon" />
              <span>製品の改変（モデルの形状変更、テクスチャの書き換え等）は自由です。</span>
            </li>
          </ul>
        </div>
      </section>

      <!-- Section 2: 禁止事項 -->
      <section class="license-section">
        <div class="section-header">
          <Icon name="mdi:alert-circle" class="section-icon prohibited" />
          <h2 class="section-title">2. 禁止事項</h2>
        </div>
        <div class="section-body">
          <ul class="prohibited-list">
            <li>
              <Icon name="mdi:close" class="list-icon" />
              <div>
                <strong>再配布・転売の禁止</strong>
                <p>改変の有無を問わず、本製品のモデルデータ、テクスチャデータそのものを再配布または販売することを禁止します。</p>
              </div>
            </li>
            <li>
              <Icon name="mdi:close" class="list-icon" />
              <div>
                <strong>自作発言の禁止</strong>
                <p>本製品またはその改変物を、自身がゼロから制作したものとして公開することを禁止します。</p>
              </div>
            </li>
          </ul>
        </div>
      </section>

      <!-- Section 6: お問い合わせ -->
      <section class="license-section">
        <div class="section-header">
          <Icon name="mdi:email" class="section-icon contact" />
          <h2 class="section-title">3. お問い合わせ</h2>
        </div>
        <div class="section-body">
          <p>
            規約に関するご質問、不具合報告などはBoothのメッセージ機能またはサポートページからお送りください。
          </p>
          <div class="contact-buttons">
            <a href="https://osaki-vrc.booth.pm/" target="_blank" rel="noopener" class="contact-button">
              <Icon name="mdi:shopping" class="button-icon" />
              Boothで確認
            </a>
            <a href="/support" class="contact-button secondary">
              <Icon name="mdi:help-circle" class="button-icon" />
              サポートページ
            </a>
          </div>
        </div>
      </section>
    </div>

    <!-- Summary Box -->
    <div class="summary-box">
      <h3 class="summary-title">要約</h3>
      <div class="summary-grid">
        <div class="summary-item allowed">
          <Icon name="mdi:check-circle" class="summary-icon" />
          <div>
            <strong>できること</strong>
            <p>メタバース空間での自由な使用、改変、収益化、動画配信への映り込み</p>
          </div>
        </div>
        <div class="summary-item prohibited">
          <Icon name="mdi:close-circle" class="summary-icon" />
          <div>
            <strong>できないこと</strong>
            <p>データの再配布・転売、自作発言、公序良俗に反する利用</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Back Link -->

  </Container>
</Layout>

<style>
  .page-header {
    text-align: center;
    padding: 4rem 0 3rem;
  }

  .page-title {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 900;
    margin-bottom: 1rem;
    color: var(--main-color);
  }

  .license-content {
    max-width: 900px;
    margin: 0 auto;
  }

  .license-section {
    background: white;
    padding: 3rem;
    border-radius: 2rem;
    border: 3px solid var(--main-color);
    margin-bottom: 3rem;
  }

  .section-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .section-icon {
    width: 2.5rem;
    height: 2.5rem;
  }

  .section-icon.allowed { color: #10b981; }
  .section-icon.prohibited { color: #ef4444; }
  .section-icon.contact { color: var(--main-color); }

  .section-title {
    font-size: 1.5rem;
    font-weight: 900;
    color: var(--sub-color);
  }

  .allowed-list li, .prohibited-list li {
    display: flex;
    gap: 1rem;
    padding: 1rem;
    border-radius: 1rem;
    margin-bottom: 1rem;
  }

  .allowed-list li { background: #f0fdf4; }
  .prohibited-list li { background: #fef2f2; }

  .list-icon { width: 1.5rem; height: 1.5rem; flex-shrink: 0; }
  .allowed-list .list-icon { color: #10b981; }
  .prohibited-list .list-icon { color: #ef4444; }

  .summary-box {
    background: var(--bg-color);
    padding: 3rem;
    border-radius: 2rem;
    border: 3px solid var(--main-color);
    margin: 4rem 0;
  }

  .summary-title {
    font-size: 1.75rem;
    font-weight: 900;
    text-align: center;
    margin-bottom: 2rem;
    color: var(--sub-color);
  }

  .summary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .summary-item {
    background: white;
    padding: 2rem;
    border-radius: 1.5rem;
    display: flex;
    gap: 1rem;
    border: 2px solid #e2e8f0;
  }

  .summary-item.allowed { border-color: #10b981; }
  .summary-item.prohibited { border-color: #ef4444; }

  .summary-icon { width: 2rem; height: 2rem; flex-shrink: 0; }
  .summary-item.allowed .summary-icon { color: #10b981; }
  .summary-item.prohibited .summary-icon { color: #ef4444; }

  .contact-buttons {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
  }

  .contact-button {
    padding: 0.75rem 2rem;
    border-radius: 3rem;
    font-weight: 900;
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .contact-button:not(.secondary) {
    background: var(--main-color);
    color: white;
  }

  .contact-button.secondary {
    border: 2px solid var(--main-color);
    color: var(--main-color);
  }

  .back-link-section {
    text-align: center;
    padding: 2rem 0;
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--main-color);
    text-decoration: none;
    font-weight: 900;
  }
</style>
